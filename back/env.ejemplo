# ============================================================
# IMPORTANTE: copiar este archivo como ".env" (con punto inicial)
# dentro de la carpeta back/ (el PASO 2 lo hace automáticamente).
# El .env verdadero NO se sube a GitHub (ya está en .gitignore).
# ============================================================
PORT=3000
DB_SERVER=localhost
DB_INSTANCE=SQLEXPRESS
DB_DATABASE=OpticaDB
DB_USER=prog2
DB_PASSWORD=
DB_ENCRYPT=false
DB_TRUST_CERT=true
